package com.experis.mefit.repository;

import com.experis.mefit.model.WorkoutComplete;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WorkoutCompleteRepository extends JpaRepository<WorkoutComplete, Long> {
}
